spring.application.name=chat
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}
server.port=${SERVER_PORT:8080}

# JWT
jwt.expiration=${JWT_EXPIRATION:86400000}

# Brevo Email
brevo.sender.email=${BREVO_SENDER_EMAIL:omnexachat@gmail.com}
brevo.sender.name=${BREVO_SENDER_NAME:Omnexa}

# PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# App behavior
chat.message.retention-days=${CHAT_MESSAGE_RETENTION_DAYS:3}
app.upload.dir=${APP_UPLOAD_DIR:uploads}
app.web.base-url=${APP_WEB_BASE_URL:http://localhost:5173}
firebase.enabled=${FIREBASE_ENABLED:true}
