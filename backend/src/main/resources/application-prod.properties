# Production config
app.cors.allowed-origin-patterns=${APP_CORS_ALLOWED_ORIGINS:https://omnexa-chat.vercel.app}

# JWT
jwt.secret=${JWT_SECRET}

# Brevo Email
brevo.api.key=${BREVO_API_KEY}

# PostgreSQL - YEH LINES ADD KARO
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# JPA / Hibernate - DIALECT ADD KARO
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.open-in-view=false
app.upload.dir=${APP_UPLOAD_DIR:uploads}

# Redis
spring.data.redis.url=${REDIS_URL}
spring.data.redis.timeout=10000ms
spring.data.redis.connect-timeout=10000ms
spring.data.redis.ssl.enabled=true
<<<<<<< HEAD
=======

# Firebase push notifications
app.web.base-url=${APP_WEB_BASE_URL:https://omnexa-chat.vercel.app}
firebase.service-account-path=${FIREBASE_SERVICE_ACCOUNT_PATH}
>>>>>>> b075237 (build a proper push notificatioin system after proper testing using firebase)
