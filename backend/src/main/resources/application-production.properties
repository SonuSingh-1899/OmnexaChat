# Production config
app.cors.allowed-origin-patterns=${APP_CORS_ALLOWED_ORIGINS:https://omnexa-flame.vercel.app,http://localhost:5173}

# JWT Configuration
jwt.secret=${JWT_SECRET:mysecreolwberljwbljbgmustbe32charslong}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Gmail SMTP Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# PostgreSQL Database
spring.datasource.url=${DB_URL:jdbc:postgresql}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:heroic}
